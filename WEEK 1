# PANDAS
its just for loading, cleaning, manipulating tabular data



# NUMPY
(number python?)
numerical operations
any calculation (+,-,*,etc)
finding averages, max/min values

example 
python
import numpy as np
numbers = np.array([2,3,6,7,9])
print(numbers + 10)
output
[12,12,16,17,19]
every each number, add + 10 *REMEMBER*

numbers = np.array([9,8,7,8])
numbers = np.array([8,7,9,8])
print(a+b)
[17,15,18,16]
IT ADDS ONE BY ONE


# MATPLOTLIB/SEABORN -> plotting????
Drawing Graphs&Charts

example

import matlplotlib.pyplot as plt ???
days = [2,3,4,5,6,7]
hours = [2,3,4,5,6,6]
plt.plot(days,hours)
plt.show()
this creates a line group



*LINE GRAPH* -> trends over time
*BAR CHART* -> compare categories
*PIE CHART* -> percentages
*SCATTER PLOT* -> show relationship

I have to train the model? but i must know
the differences???
any unique patterns???


# MACHINE LEARNING ???

collect data -> pandas (read and organise it) -> NumPy (visualise it or more like counting it?) -> Scikit-lesrn (train a model) -> TensorFlow/PyTorch (deep learning? whew)
